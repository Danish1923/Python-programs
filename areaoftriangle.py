#collecting user input
base=float(input("Enter the base value:"))
height=float(input("Enter the height:"))

area=0.5* base*height #formula (use 1/2 or 0.5 is same)

#printing result

print(f"The area of Triangle is {area:.2f}")